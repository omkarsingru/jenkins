node{
     stage ('SCM Checkout') {
 git 'https://github.com/omkarsingru/jenkins'
 }
 stage ('Compile-Package'){
 def mvnHome =  tool name: 'AngularspringbootMAVEN', type: 'maven'
 sh "${MmvnHome}/bin/mvn package"
 }
 }
